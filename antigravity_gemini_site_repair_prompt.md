# Prompt إصلاح موقع Diwan AI داخل Antigravity IDE

## طريقة الاستخدام

انسخ أولًا قسم **Prompt التشغيل الرئيسي** كاملًا والصقه داخل Antigravity مع فتح مجلد المشروع الصحيح. لا تطلب من Gemini تنفيذ كل الإصلاحات في رسالة واحدة بعد ذلك. بعد أن ينتهي من التشخيص ويعرض التقرير، استخدم أوامر المتابعة الموجودة في نهاية هذا الملف مرحلةً بعد مرحلة.

استخدم Gemini 3.1 Pro في التشخيص، فهم بنية المشروع، القرارات المعمارية، وإصلاح المشاكل الحساسة. استخدم Gemini Flash 3.7 فقط للإصلاحات الصغيرة المتكررة، تنسيق الملفات، كتابة الاختبارات البسيطة، أو إعادة فحص الأخطاء بعد أن تكون الخطة واضحة.

---

# Prompt التشغيل الرئيسي — انسخه كما هو

أنت الآن تعمل كمهندس صيانة Senior Full-Stack ومهندس QA لمشروع ويب موجود مسبقًا. مهمتك ليست إعادة بناء الموقع من الصفر، وليست تغيير التصميم عشوائيًا. مهمتك هي **فهم المشروع أولًا، اكتشاف الأخطاء الحقيقية، ترتيبها حسب الخطورة، ثم إصلاحها تدريجيًا مع الحفاظ على الميزات الحالية**.

اسم المنتج الذي نريد اعتماده في الواجهة هو: **Diwan AI**. إذا وجدت أسماء أخرى مثل `VANTRA` أو `VANTRA STUDIO` أو `zap` داخل واجهة المستخدم، فلا تستبدلها مباشرة قبل أن تحدد أماكنها وتأثيرها. اعتبر أن توحيد الهوية جزء من الإصلاح، لكن لا تغيّر أي اسم داخل منطق الخادم أو قاعدة البيانات أو التكاملات قبل التأكد من أنه آمن.

الموقع عبارة عن منصة تجمع نماذج الذكاء الاصطناعي في مكان واحد، وتشمل على الأقل: المحادثة، توليد الصور، توليد الفيديو، الرصيد والنقاط، الجلسات السابقة، الدفع المحلي، وواجهات API/Webhooks للمطورين. يجب أن تبقى هذه الوظائف منفصلة وواضحة للمستخدم، وألا يؤدي إصلاح واجهة إلى كسر أي تدفق خلفي.

## قواعد إلزامية قبل تعديل أي ملف

1. ابدأ بفحص المشروع فقط. لا تعدّل الملفات في أول استجابة.
2. اكتشف نوع المشروع، أوامر التشغيل، أوامر الاختبار، بنية المجلدات، الصفحات، المكونات، الخدمات الخلفية، مخططات قاعدة البيانات، ملفات البيئة، ومسارات API.
3. لا تعرض أي قيمة سرية من ملفات `.env` أو مفاتيح API أو بيانات المستخدمين. اذكر أسماء المتغيرات فقط دون قيمها.
4. لا تفترض أن الخطأ سببه الواجهة. افحص أيضًا حالة البيانات، الاستدعاءات الخلفية، التحقق من المدخلات، الصلاحيات، حالات التحميل، وأخطاء الشبكة.
5. لا تحذف ميزة أو ملفًا أو مسارًا أو dependency إلا بعد بيان سبب واضح والتأكد من عدم وجود استخدام له.
6. لا تغيّر الأسعار أو سياسات الدفع من التخمين. إذا وجدت أسعارًا مختلفة بين الصفحة العامة وStudio، سجّل التعارض أولًا وأنشئ مصدرًا واحدًا للبيانات بعد تحديد القيم المعتمدة من الملفات الحالية.
7. لا تنفذ إعادة تصميم شاملة الآن. أصلح الأخطاء الوظيفية أولًا، ثم أخطاء تجربة الاستخدام، ثم التحسينات البصرية.
8. لا تستخدم بيانات تجريبية مكان بيانات حقيقية دون وضع علامة واضحة، ولا تجعل زرًا يبدو فعالًا إذا لم يكن له تنفيذ حقيقي.
9. بعد كل مجموعة صغيرة من التغييرات، شغّل lint أو typecheck أو test أو build المتاح، وسجّل النتيجة.
10. إذا احتجت معلومة لا تستطيع استنتاجها بأمان، توقف واسألني سؤالًا محددًا بدل اختراع حل.

## المرحلة الأولى: إنشاء تقرير تشخيص فقط

افحص المشروع وأنشئ تقريرًا منظمًا دون تغيير الكود. يجب أن يحتوي التقرير على الجداول التالية:

### أ. خريطة المشروع

اعرض أهم المجلدات والملفات، وظيفة كل ملف، والملفات الحساسة التي يجب عدم تعديلها دون حذر.

### ب. خريطة الصفحات والمسارات

اذكر كل route أو page، وما الذي يظهر فيه، وهل هو Landing Page أو Studio أو صفحة دفع أو صفحة مطورين.

### ج. خريطة الوظائف

اربط كل وظيفة بمكان تنفيذها. يجب فحص Chat، Image Generator، Video Engine، اختيار النموذج، رفع الملفات، الجلسات، الرصيد، Top up، API Keys، Webhooks، تسجيل الدخول، وتبديل اللغة.

### د. جدول الأخطاء

استخدم الأعمدة التالية:

| ID | الخطأ أو الخلل | الدليل من الكود أو التشغيل | الخطورة | الملف/المسار | الحل المقترح | هل يحتاج موافقتي؟ |
| --- | --- | --- | --- | --- | --- | --- |

صنّف الخطورة إلى:

- **P0:** يمنع التشغيل أو يسبب فقدان بيانات أو خطأ دفع أو خللًا أمنيًا.
- **P1:** يكسر وظيفة أساسية أو يجعل المستخدم غير قادر على إكمال المهمة.
- **P2:** مشكلة واجهة أو تجربة استخدام مهمة لكنها لا تمنع التشغيل.
- **P3:** تحسين بصري أو تنظيمي غير عاجل.

### هـ. فحوص التشغيل

شغّل الأوامر المناسبة للمشروع، مثل:

```bash
npm run lint
npm run typecheck
npm run test
npm run build
```

لا تشغّل أمرًا غير موجود. إذا فشل أمر، اعرض الخطأ الحقيقي كما هو، ثم اربطه بالملف والسبب المحتمل. لا تعتبر التحذير خطأً قاتلًا إلا بعد التحقق.

### و. فحص عدم الاتساق

ابحث صراحة عن:

- `Diwan AI` و`VANTRA` و`VANTRA STUDIO` و`zap` وأي اسم تجاري آخر.
- فروقات أسعار النماذج بين Landing Page وStudio وCost Table وBilling.
- اختلاف `Free Plan` و`Pro Plan` في نفس الحساب أو الشاشة.
- النصوص الإنجليزية غير المترجمة والنصوص العربية غير المكتملة.
- `lang="en"` أو غياب `dir="rtl"` في النسخة العربية.
- أزرار لا تنفذ شيئًا، روابط مكسورة، صور placeholder، وبيانات demo غير المعلّمة.
- حالات loading وempty وerror وsuccess لكل وظيفة أساسية.
- اختلاف أسماء النماذج أو المزوّدين بين البطاقات والقوائم وواجهات التنفيذ.
- أي CSS يسبب overflow أو قصًا في الهاتف أو مشاكل في القوائم المنسدلة.

بعد التقرير، **توقف ولا تعدّل شيئًا**. انتظر موافقتي على خطة الإصلاح.

## النتيجة المطلوبة في نهاية المرحلة الأولى

أعطني:

1. ملخصًا لأكبر 10 أخطاء.
2. جدولًا كاملًا للأخطاء حسب P0/P1/P2/P3.
3. خطة إصلاح من 3 إلى 6 مراحل.
4. الملفات التي ستتغير في كل مرحلة.
5. المخاطر والوظائف التي يجب اختبارها بعد كل مرحلة.
6. أي سؤال واحد أو أكثر لا يمكن حسمه من الكود.

لا تكتب كودًا في هذه المرحلة.

---

# أوامر المتابعة بعد تقرير التشخيص

## الأمر 1 — إصلاح أخطاء التشغيل والبناء

بعد أن يعرض التقرير، الصق:

> ابدأ الآن بإصلاح أخطاء **P0 الخاصة بالتشغيل والبناء والأنواع والاستدعاءات الخلفية فقط**. لا تغيّر الهوية البصرية أو الأسعار أو النصوص في هذه المرحلة. نفّذ تغييرًا صغيرًا كل مرة، ثم شغّل lint/typecheck/test/build المتاح. بعد كل تغيير، اذكر الملف الذي تغيّر، سبب التغيير، ونتيجة الفحص. إذا ظهر خطأ جديد، عالجه قبل الانتقال. لا تعتبر المرحلة مكتملة إلا إذا كان أمر build ينجح أو تشرح بوضوح الخطأ الخارجي الذي يمنعه.

## الأمر 2 — إصلاح الوظائف الأساسية

> أصلح الآن أخطاء P1 في الوظائف الأساسية: إنشاء جلسة Chat، اختيار النموذج، إرسال الطلب، حالات التحميل، عرض الخطأ، توليد الصور، توليد الفيديو، حفظ الجلسات، وخصم النقاط. لا تغيّر API contract أو أسماء حقول قاعدة البيانات دون فحص جميع المستهلكين. لكل وظيفة، اختبر الحالة الطبيعية، الحالة الفارغة، فشل الشبكة، الرصيد غير الكافي، والضغط المتكرر على زر التنفيذ. اجعل الأزرار المعطلة والرسائل واضحة، ولا تعرض نجاحًا وهميًا.

## الأمر 3 — توحيد الهوية والبيانات الحساسة للثقة

> أصلح الآن عدم الاتساق في الهوية والبيانات الظاهرة للمستخدم. استخدم اسم `Diwan AI` في الواجهة إذا لم يوجد تعارض تقني آمن. استبدل `zap` من النصوص الظاهرة للمستخدم. وحّد عناوين الصفحة، الشعار، اسم Studio، ورسائل empty state. لا تعدّل قيم الأسعار من التخمين؛ ابحث عن كل مصادر الأسعار، أنشئ مصدرًا مركزيًا واحدًا، وأظهر تقريرًا بالفروقات قبل اعتماد قيمة نهائية. وحّد عرض الخطة والرصيد، ولا تعرض `Free Plan` و`Pro Plan` في نفس الحساب دون تفسير.

> يجب أن تكون كل قيمة سعر مرتبطة بوحدة واضحة: لكل طلب، لكل 1,000 token، لكل صورة، أو لكل 5 ثوانٍ من الفيديو. يجب أن تظهر التكلفة المتوقعة قبل التنفيذ، والتكلفة الفعلية بعده، والرصيد المتبقي بعد العملية.

## الأمر 4 — إصلاح اللغة العربية وRTL

> نفّذ دعمًا صحيحًا للعربية والفرنسية والإنجليزية باستخدام نظام ترجمة مركزي بدل النصوص المبعثرة داخل المكونات. عند اختيار العربية، فعّل `dir="rtl"` و`lang="ar"` على المستوى المناسب، واضبط اتجاه الحقول والأزرار والقوائم والرسائل. لا تعرّب أسماء النماذج أو الشركات إلا إذا كان هناك اسم عربي معتمد. ترجم رسائل التحميل والخطأ والنجاح والدفع والرصيد. اختبر النصوص الطويلة على الهاتف، وتأكد من عدم قصها أو انعكاس الأيقونات بطريقة غير صحيحة.

## الأمر 5 — تحسين Studio بدون كسر المنطق

> حسّن شاشة Studio مع الحفاظ على المنطق الخلفي الحالي. اجعل نقطة البداية موجهة بالمهمة بدل محرر فارغ. أضف أو حسّن بطاقات: `دردشة وكتابة`، `برمجة وتحليل`، `توليد صورة`، `توليد فيديو`، و`تحويل صوت إلى نص` إذا كانت الوظيفة متاحة فعلًا. اجعل كل بطاقة تنقل إلى التدفق الصحيح. أظهر النموذج المختار، سبب التوصية، تكلفة التنفيذ، والرصيد المتوقع. انقل `Clone UI` و`Import Figma` و`Create Page` إلى قسم مستقل مثل `Build` إذا كانت وظائف حقيقية، ولا تعرضها في الواجهة الأساسية إذا لم تكن مكتملة.

> أصلح empty states، loading states، error states، responsive layout، focus states، keyboard navigation، والتباين. لا تضف animation لمجرد الشكل، ولا تستخدم placeholder تجريبيًا على أنه نتيجة حقيقية.

## الأمر 6 — إصلاح الهاتف وإتاحة الاستخدام

> اختبر الموقع على أحجام عرض 360px و390px و430px وعلى شاشة سطح مكتب. افحص الشريط الجانبي، زر فتح القائمة، textarea، اختيار النموذج، الأزرار، البطاقات، الصور، الفيديو، الجداول، والنوافذ المنبثقة. أصلح أي horizontal overflow أو قص أو تداخل. حافظ على حد أدنى مناسب لحجم النقر، وتركيز لوحة المفاتيح، وlabels للحقول، وaria-label للأزرار التي تحتوي على أيقونة فقط. لا تعتبر الصفحة متجاوبة لمجرد وجود media query؛ تحقق من كل تدفق أساسي.

## الأمر 7 — الاختبار النهائي والتنظيف

> نفّذ الآن مراجعة نهائية. شغّل lint/typecheck/test/build، ثم افحص كل route أساسي. أنشئ checklist يتضمن: Landing Page، تسجيل الدخول، Chat، اختيار النموذج، Image Generator، Video Engine، الرصيد، Top up، الجلسات، اللغة العربية، الهاتف، API Keys، Webhooks، ورسائل الخطأ. لا تغلق أي مهمة بعبارة «تم» دون دليل من الكود أو نتيجة اختبار. اعرض الملفات التي تغيرت، وأي ملاحظات متبقية، وأي خطوة تحتاج تدخلي اليدوي مثل مفاتيح البيئة أو اختبار الدفع الحقيقي.

---

# قواعد خاصة عند ظهور خطأ جديد

عندما أرسل لك خطأ من Console أو Terminal، لا تعِد كتابة المشروع. اتبع هذا القالب:

1. حدّد هل الخطأ من Frontend أو Backend أو Database أو Environment أو Network.
2. اذكر السبب الأقرب مع الدليل.
3. اذكر الملف والسطر أو المكوّن المسؤول.
4. اقترح أصغر تعديل آمن.
5. نفّذ التعديل.
6. شغّل الفحص المناسب.
7. اذكر إن كان التعديل يؤثر على وظائف أخرى.

إذا كانت الرسالة مثل `Module not found` أو `Cannot read properties of undefined` أو `Hydration failed` أو `401/403/500`، لا تعالج العرض فقط؛ تتبع مصدر القيمة أو الاستدعاء حتى السبب الأساسي.

# ما لا أريد منك فعله

لا تعِد بناء التطبيق من الصفر. لا تغيّر framework أو قاعدة البيانات أو نظام الدفع. لا تثبّت dependencies جديدة دون سبب. لا تحذف ملفات تبدو غير مستخدمة قبل البحث عن imports وroutes وdynamic imports. لا تضع مفاتيح API داخل الواجهة. لا تتجاوز التحقق من الرصيد أو الصلاحيات. لا تنفذ عملية دفع حقيقية أو تغيّر webhook production أثناء الاختبار. لا تستبدل بيانات حقيقية ببيانات ثابتة فقط لإخفاء الخطأ. لا تخلط إصلاح bug مع إعادة تصميم شاملة في commit واحد.

# صيغة التقرير بعد كل مرحلة

في نهاية كل مرحلة، أجب بهذا الشكل:

```text
المرحلة:

الهدف:

الملفات التي تغيرت:

المشاكل التي تم إصلاحها:

المشاكل التي لم تُصلح والسبب:

الأوامر التي تم تشغيلها ونتائجها:

الوظائف التي تم اختبارها:

المخاطر أو الآثار الجانبية المحتملة:

هل تحتاج إلى موافقتي قبل المرحلة التالية؟ نعم/لا
```

# ملاحظة مهمة للمستخدم

إذا رفض Gemini تنفيذ الخطة وقال إنه يحتاج معلومات، لا ترسل له شرحًا طويلًا جديدًا. أرسل له فقط: «اعرض لي الملفات أو القيم التي تحتاجها في سؤالين محددين». وإذا حاول تعديل ملفات كثيرة دفعة واحدة، أوقفه واستخدم: «تراجع عن هذه المرحلة، نفّذ فقط أول خطأ P0، ثم شغّل الفحص وأرني النتيجة».
