# VANTRA — SELF WORK RULES (متعهد أنفذها في كل تعديل)

## القاعدة الذهبية
كل تعديل واجهة = إصدار جديد مرئي + إثبات نشر. ممنوع القول "تم" بدون إثبات.

## البروتوكول الإلزامي لكل تعديل
1. **اقرأ الملف الفعلي** قبل التعديل (ليس من الذاكرة).
2. **حدد السبب الجذري** بالتحديد (أي عنصر، أي خاصية، أي تعارض) — ممنوع تعديلات تخمينية متكدسة.
3. **ارفع رقم الإصدار** في `DashboardSidebar.tsx` → `Studio vX.Y` — هذا هو جرس التحقق للمستخدم.
4. **ابنِ محلياً**: `npx tsc --noEmit` ثم `npm run build` — صفر أخطاء وإلا لا نشر.
5. **انشر**: `vercel --prod --yes` وتحقق من `"readyState": "READY"` في اللوج.
6. **أثبت الحياة**: اجلب `https://ai-alpha-delta-six.vercel.app/studio` وتحقق أن HTML يحوي:
   - رقم الإصدار الجديد
   - الكلاس/العنصر المحدد الذي عدّلته
7. **ذكّر المستخدم دائماً**: `Ctrl+Shift+R` + التحقق من رقم الإصدار تحت الشعار قبل الحكم.
8. عند فشل النشر (fetch failed / not authorized): أعد المحاولة أو أعد تسجيل الدخول — لا تعتبره منجزاً.

## قواعد التصميم الثابتة (Nardo & Champagne v5+)
- الخلفية #16181A / الأسطح #212326 / Accent ذهبي #E6C27A فقط — ممنوع إعادة ألوان النيون (سماوي/بنفسجي/تيل).
- نص أساسي #FFFFFF، ثانوي #A1A5AB، حدود rgba(255,255,255,0.08-0.12).
- الشات بوكس: عنصر in-flow تحت منطقة المحادثة (ممنوع الرجوع للـ overlay المطلق).
- أزرار الإيقاع: active:scale-95، انتقالات 200-300ms.
- RTL/LTR: `detectDir` لكل رسالة + `unicode-bidi: plaintext` — لا تكسر هذا النظام.

## أخطاء الماضي — لا تكررها
- زر شبه مخفي (bg 7% + نص 25%) → أي زر يجب أن يكون مرئياً بوضوح في كل حالاته.
- حلقات ring/ظلال ترسم خارج حدود العنصر داخل إطارات مدورة → تبدو "خروج من الإطار".
- حشوات تخمينية بدون قياس → استخدم قيماً واضحة وتحقق بالـ HTML المبني.
- overlay مطلق فوق منطقة المحادثة → سبب مشكلة "النص تحت الصندوق" لـ 5 إصدارات.
